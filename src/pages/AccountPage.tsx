import { useEffect, useState, type FormEvent } from 'react';
import { useAuth } from '../auth/AuthProvider';
import { authErrorMessage } from '../auth/auth-errors';
import { loadProfile, saveProfile, type CustomerProfile } from '../auth/profile-service';
export function AccountPage() { const { user, signOut } = useAuth(); const [profile, setProfile] = useState<CustomerProfile | null>(null); const [status, setStatus] = useState(''); useEffect(() => { if (user) loadProfile(user.id).then(setProfile).catch((error) => setStatus(authErrorMessage(error))); }, [user]); if (!user) return null; if (!profile) return <main className="empty-page"><p className="eyebrow">Личный кабинет</p><h1>Загружаем профиль…</h1><p>{status}</p></main>; function change(field: keyof Omit<CustomerProfile, 'id'>, value: string) { setProfile((current) => current ? { ...current, [field]: value } : current); } async function submit(event: FormEvent) { event.preventDefault(); setStatus('Сохраняем…'); try { await saveProfile(profile!); setStatus('Данные сохранены.'); } catch (error) { setStatus(authErrorMessage(error)); } } return <main className="page account-page"><header className="page-heading"><p className="eyebrow">Личный кабинет</p><h1>Ваш профиль</h1><p>{user.email}</p></header><div className="account-layout"><form className="checkout-form" onSubmit={submit}><div className="form-heading"><span>01</span><h2>Контакты для заказа</h2></div><div className="form-grid"><ProfileField label="Имя" value={profile.name} onChange={(v) => change('name', v)} /><ProfileField label="Телефон" value={profile.phone} onChange={(v) => change('phone', v)} /><ProfileField label="Город" value={profile.city} onChange={(v) => change('city', v)} /><ProfileField label="Адрес или пункт выдачи" value={profile.address} onChange={(v) => change('address', v)} /></div><button className="button" type="submit">Сохранить изменения</button><p className="auth-status" aria-live="polite">{status}</p></form><aside className="account-aside"><p className="eyebrow">Аккаунт</p><p>Сохранённые данные подставятся при следующем оформлении заказа.</p><button className="button button--outline" type="button" onClick={() => signOut()}>Выйти</button></aside></div></main>; }
function ProfileField({ label, value, onChange }: { label: string; value: string; onChange: (value: string) => void }) { return <label className="field"><span>{label}</span><input value={value} onChange={(event) => onChange(event.target.value)} /></label>; }
